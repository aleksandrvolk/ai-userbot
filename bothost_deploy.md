# 🚀 Развертывание на Bothost.ru

## 📋 Подготовка к развертыванию

### Шаг 1: Подготовьте код

Убедитесь, что у вас есть все необходимые файлы:
- `userbot.py` - основной файл бота
- `config.py` - конфигурация
- `database.py` - работа с БД
- `requirements.txt` - зависимости

> 💡 **Примечание:** Dockerfile генерируется автоматически на Bothost.ru, не нужно добавлять его в репозиторий.

### Шаг 2: Настройте переменные окружения

Вам понадобятся следующие переменные (настройте их в панели Bothost.ru):

```
API_ID=ваш_api_id
API_HASH=ваш_api_hash
SESSION_NAME=userbot_session
DATABASE_PATH=/app/data/messages.db
LOG_LEVEL=INFO
LOG_FILE=/app/data/userbot.log
```

## 🔧 Развертывание на Bothost.ru

### Вариант 1: Через Git-репозиторий (Рекомендуется)

1. **Создайте Git-репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <ваш_repo_url>
   git push -u origin main
   ```

2. **На Bothost.ru:**
   - Создайте новый проект
   - Выберите "Telegram Bot"
   - Подключите ваш Git-репозиторий
   - Укажите ветку (обычно `main` или `master`)

3. **Настройте переменные окружения:**
   - Перейдите в настройки проекта
   - Добавьте все переменные из списка выше
   - Сохраните

4. **Запустите развертывание:**
   - Нажмите "Развернуть" или "Deploy"
   - Дождитесь завершения сборки

### Вариант 2: Через веб-интерфейс

1. **Подготовьте архив:**
   - Создайте ZIP-архив со всеми файлами проекта
   - Исключите: `.env`, `*.db`, `*.log`, `*.session`

2. **На Bothost.ru:**
   - Создайте новый проект
   - Выберите "Загрузить файлы"
   - Загрузите ZIP-архив
   - Распакуйте в корневую директорию

3. **Настройте переменные окружения** (как в варианте 1)

4. **Запустите развертывание**

## ⚙️ Настройка проекта на Bothost.ru

### Основные настройки:

1. **Тип приложения:** Python
2. **Команда запуска:** `python userbot.py`
3. **Рабочая директория:** `/app`
4. **Порт:** Не требуется (userbot не использует HTTP)

### Переменные окружения:

Добавьте в настройках проекта:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `API_ID` | Ваш API ID | Из my.telegram.org/apps |
| `API_HASH` | Ваш API Hash | Из my.telegram.org/apps |
| `SESSION_NAME` | `userbot_session` | Имя файла сессии |
| `DATABASE_PATH` | `/app/data/messages.db` | Путь к БД |
| `LOG_LEVEL` | `INFO` | Уровень логирования |
| `LOG_FILE` | `/app/data/userbot.log` | Файл логов |

### Ресурсы:

Рекомендуемые настройки:
- **RAM:** Минимум 512 MB (для больших групп - 1 GB)
- **CPU:** 0.5-1 CPU
- **Диск:** 2-5 GB (зависит от объема данных)

## 🔐 Первый запуск и авторизация

### Важно: Авторизация через сессию

Userbot требует авторизацию через Telegram. Есть два способа:

### Способ 1: Локальная авторизация (Рекомендуется)

1. **Запустите userbot локально один раз:**
   ```bash
   python userbot.py
   ```
   
2. **Авторизуйтесь:**
   - Введите номер телефона
   - Введите код подтверждения
   - Сессия сохранится в файл `userbot_session.session`

3. **Загрузите файл сессии на Bothost:**
   - Скопируйте `userbot_session.session` и `userbot_session.session-journal` (если есть)
   - Загрузите их в корневую директорию проекта на Bothost
   - Или используйте переменную окружения для пути

### Способ 2: Авторизация на сервере (Сложнее)

1. **Включите SSH доступ** (если доступен на вашем тарифе)
2. **Подключитесь к серверу**
3. **Запустите userbot вручную** для авторизации
4. **Сессия сохранится автоматически**

## 📝 Проверка работы

После развертывания:

1. **Проверьте логи:**
   - В панели Bothost откройте раздел "Логи"
   - Должно быть: `"Userbot запущен и готов к работе!"`

2. **Проверьте статус:**
   - Статус должен быть "Running" (Запущен)

3. **Протестируйте команды:**
   - Напишите userbot в личные сообщения
   - Отправьте `/help`
   - Должен прийти ответ

## 🔄 Обновление бота

### При использовании Git:

1. **Внесите изменения в код**
2. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. **Bothost автоматически пересоберет и перезапустит бота**

### При ручной загрузке:

1. **Обновите файлы**
2. **Загрузите новую версию**
3. **Перезапустите проект** в панели Bothost

## 🗄️ Работа с базой данных

База данных сохраняется в `/app/data/messages.db`.

### Резервное копирование:

Bothost Pro включает автоматическое резервное копирование. Для других тарифов:

1. **Экспортируйте данные:**
   ```bash
   python export_data.py json backup.json
   ```

2. **Скачайте файл БД:**
   - Через SSH или веб-интерфейс файлов
   - Скопируйте `/app/data/messages.db`

### Восстановление:

1. **Загрузите файл БД** в `/app/data/`
2. **Перезапустите бота**

## 📊 Мониторинг

Bothost предоставляет:

- **Логи в реальном времени**
- **Статистику использования ресурсов**
- **Метрики производительности**
- **Уведомления об ошибках**

Используйте панель для мониторинга работы бота.

## ⚠️ Важные замечания

1. **Файл сессии:**
   - Храните `*.session` файлы в безопасности
   - Не коммитьте их в Git
   - Регулярно делайте резервные копии

2. **Переменные окружения:**
   - Никогда не публикуйте `.env` файлы
   - Используйте переменные окружения в панели Bothost

3. **Ресурсы:**
   - Следите за использованием RAM и диска
   - При необходимости увеличьте тариф

4. **Логи:**
   - Регулярно проверяйте логи на ошибки
   - Настройте уведомления в Bothost

## 🐛 Решение проблем

### Бот не запускается:

1. Проверьте логи в панели Bothost
2. Убедитесь, что все переменные окружения установлены
3. Проверьте наличие файла сессии

### Ошибка авторизации:

1. Убедитесь, что файл сессии загружен
2. Проверьте правильность API_ID и API_HASH
3. Попробуйте пересоздать сессию

### Недостаточно памяти:

1. Увеличьте тарифный план
2. Оптимизируйте код
3. Очистите старые данные

## 📞 Поддержка

- **Документация Bothost:** https://docs.bothost.ru/
- **Поддержка Bothost:** Через панель управления
- **Логи проекта:** В панели Bothost → Логи

## ✅ Чек-лист развертывания

- [ ] Код загружен на Bothost
- [ ] Все переменные окружения настроены
- [ ] Файл сессии загружен (или авторизация пройдена)
- [ ] Бот запущен и работает
- [ ] Логи показывают успешный запуск
- [ ] Команды отвечают корректно
- [ ] Настроено резервное копирование
- [ ] Мониторинг активен

Готово! Ваш userbot развернут на Bothost.ru 🎉

