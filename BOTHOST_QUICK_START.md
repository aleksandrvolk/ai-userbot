# ⚡ Быстрый старт на Bothost.ru

## 🚀 Развертывание за 5 минут

### Шаг 1: Подготовка (локально)

1. **Авторизуйтесь один раз локально:**
   ```bash
   python userbot.py
   ```
   - Введите номер телефона
   - Введите код подтверждения
   - Файл `userbot_session.session` будет создан

2. **Проверьте, что файлы готовы:**
   ```bash
   # Linux/Mac
   chmod +x deploy.sh
   ./deploy.sh
   
   # Windows (PowerShell)
   # Просто проверьте наличие всех файлов
   ```

### Шаг 2: Загрузка на Bothost.ru

#### Вариант A: Через Git (Рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - См. инструкцию: [GITHUB_SETUP.md](GITHUB_SETUP.md)
   - Или используйте существующий: `https://github.com/aleksandrvolk/ai-userbot`

2. **На Bothost.ru:**
   - Создайте проект → Подключите Git
   - Укажите: `https://github.com/aleksandrvolk/ai-userbot`
   - Выберите ветку: `main`
   - Dockerfile сгенерируется автоматически

#### Вариант B: Через ZIP

1. **Создайте ZIP-архив** (исключите `.env`, `*.db`, `*.log`, `*.session`)
2. **Загрузите на Bothost.ru** через веб-интерфейс

### Шаг 3: Настройка переменных окружения

В панели Bothost.ru → Настройки проекта → Переменные окружения:

```
API_ID=ваш_api_id
API_HASH=ваш_api_hash
SESSION_NAME=userbot_session
DATABASE_PATH=/app/data/messages.db
LOG_LEVEL=INFO
LOG_FILE=/app/data/userbot.log
```

### Шаг 4: Загрузка файла сессии

**Важно:** Файл сессии нужен для авторизации!

1. **Скопируйте файлы:**
   - `userbot_session.session`
   - `userbot_session.session-journal` (если есть)

2. **Загрузите на Bothost:**
   - Через веб-интерфейс файлов
   - Или через Git (добавьте в репозиторий, но это небезопасно!)

   ⚠️ **Безопаснее:** Используйте SSH для загрузки файла сессии

### Шаг 5: Запуск

1. Нажмите **"Развернуть"** или **"Deploy"**
2. Дождитесь завершения сборки
3. Проверьте логи - должно быть: `"Userbot запущен и готов к работе!"`

## ✅ Проверка работы

1. **Откройте логи** в панели Bothost
2. **Напишите userbot** в личные сообщения
3. **Отправьте:** `/help`
4. **Должен прийти ответ**

## 🔧 Если что-то не работает

### Бот не запускается:
- ✅ Проверьте все переменные окружения
- ✅ Убедитесь, что файл сессии загружен
- ✅ Проверьте логи на ошибки

### Ошибка авторизации:
- ✅ Пересоздайте сессию локально
- ✅ Загрузите новый файл сессии

### Недостаточно памяти:
- ✅ Увеличьте тарифный план
- ✅ Используйте `limit=` при парсинге

## 📚 Подробная инструкция

См. [bothost_deploy.md](bothost_deploy.md) для детальной информации.

## 🎯 Готово!

Ваш userbot работает на Bothost.ru 24/7! 🎉

