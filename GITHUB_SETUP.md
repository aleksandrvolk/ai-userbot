# 📦 Настройка GitHub репозитория

## 🚀 Быстрая настройка

### Шаг 1: Инициализация Git

```bash
# Инициализируйте репозиторий
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Telegram userbot for message parsing"
```

### Шаг 2: Подключение к GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `ai-userbot` (или любое другое)
   - Выберите "Public" или "Private"
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

2. **Подключите локальный репозиторий:**
   ```bash
   git remote add origin https://github.com/aleksandrvolk/ai-userbot.git
   git branch -M main
   git push -u origin main
   ```

### Шаг 3: Проверка

Проверьте, что на GitHub загружены:
- ✅ `userbot.py`
- ✅ `config.py`
- ✅ `database.py`
- ✅ `requirements.txt`
- ✅ `README.md`
- ✅ Все файлы документации
- ❌ **НЕТ** `.env` файла
- ❌ **НЕТ** `*.session` файлов
- ❌ **НЕТ** `*.db` файлов
- ❌ **НЕТ** `Dockerfile` (генерируется автоматически)

## 📝 Структура репозитория

```
ai-userbot/
├── userbot.py              # Основной файл бота
├── config.py               # Конфигурация
├── database.py             # Работа с БД
├── export_data.py          # Экспорт данных
├── requirements.txt        # Зависимости
├── README.md               # Основная документация
├── .gitignore             # Исключения для Git
│
├── Документация:
├── BOTHOST_QUICK_START.md  # Быстрый старт на Bothost
├── bothost_deploy.md       # Развертывание на Bothost
├── DEPLOY_CHECKLIST.md     # Чек-лист развертывания
├── FAQ.md                  # Часто задаваемые вопросы
├── HOW_TO_ADD_TO_GROUP.md  # Добавление в группы
├── ПАРСИНГ_ГРУПП_КОНКУРЕНТОВ.md
├── USERBOT_VS_BOT.md       # Сравнение userbot и бота
├── БЫСТРЫЙ_СТАРТ.md        # Быстрый старт
│
└── Скрипты:
    └── deploy.sh           # Скрипт проверки готовности
```

## ⚠️ Важно: Что НЕ должно быть в репозитории

Следующие файлы **НЕ должны** быть загружены в GitHub:

- ❌ `.env` - содержит секретные ключи
- ❌ `*.session` - файлы сессии Telegram
- ❌ `*.db` - базы данных
- ❌ `*.log` - логи
- ❌ `Dockerfile` - генерируется автоматически на Bothost
- ❌ `.dockerignore` - не нужен, если нет Dockerfile

Все эти файлы уже добавлены в `.gitignore`.

## 🔄 Обновление репозитория

После внесения изменений:

```bash
# Проверьте статус
git status

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

## 🔗 Подключение к Bothost.ru

После загрузки на GitHub:

1. **На Bothost.ru:**
   - Создайте новый проект
   - Выберите "Подключить Git-репозиторий"
   - Укажите: `https://github.com/aleksandrvolk/ai-userbot`
   - Выберите ветку: `main`

2. **Bothost автоматически:**
   - Скачает код
   - Сгенерирует Dockerfile
   - Установит зависимости
   - Запустит бота

## 📚 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- <файл>

# Посмотреть историю
git log

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main

# Удалить ветку
git branch -d <название_ветки>
```

## 🐛 Решение проблем

### Ошибка: "remote origin already exists"

```bash
# Удалите существующий remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/aleksandrvolk/ai-userbot.git
```

### Ошибка: "failed to push"

```bash
# Получите последние изменения
git pull origin main --rebase

# Попробуйте снова
git push
```

### Случайно закоммитили секретные файлы

```bash
# Удалите из истории (осторожно!)
git rm --cached .env
git rm --cached *.session

# Обновите .gitignore
# Создайте новый коммит
git commit -m "Remove sensitive files"

# Если уже отправили на GitHub:
# 1. Смените все секретные ключи
# 2. Удалите файлы из истории (git filter-branch)
# 3. Или создайте новый репозиторий
```

## ✅ Чек-лист перед отправкой

- [ ] Все секретные файлы в `.gitignore`
- [ ] `Dockerfile` не включен (генерируется автоматически)
- [ ] README.md актуален
- [ ] Все зависимости в `requirements.txt`
- [ ] Код протестирован локально
- [ ] Коммиты имеют понятные сообщения

## 🎯 Готово!

Ваш репозиторий готов к использованию на Bothost.ru! 🎉

